import create from './app';
import Router from 'koa-router';
import mongoose from 'mongoose';

export {
    create,
    Router,
    mongoose
};

export default {
    create,
    Router,
    mongoose
};
